# AgentCore Runtime (BYO) dependencies.
# The SDK provides BedrockAgentCoreApp + the :8080 serving contract.
bedrock-agentcore>=0.1.0
# The runtime packages the backend retrieval agent, so it shares its deps.
boto3>=1.34.0
psycopg[binary]>=3.2.0
pydantic>=2.7.0
python-dotenv>=1.0.1
numpy>=1.26.0
