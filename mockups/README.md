# Mockups

`latest-static-prototype.html` is a self-contained static prototype derived from the latest light-theme UI direction. It is intended for design review only.

The prototype preserves older static-copy labels, including ServiceNow references. The canonical app, seed data, and workshop path use Slack, Jira, Confluence, Salesforce, and GitHub only.

Security-review changes applied:

- Removed remote Google Fonts references.
- Uses the simple text-only product name: **Verity**.
- Connector logo assets are local static files only.
