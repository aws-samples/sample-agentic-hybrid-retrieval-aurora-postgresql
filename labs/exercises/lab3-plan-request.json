{
  "question": "For {{INCIDENT_KEY}}, why did some order writes block inside PostgreSQL while others timed out before reaching it, why did only the blocked writers recover when the unbatched priority_tier backfill in {{UNSAFE_CHANGE_KEY}} committed, and why did {{ANALYZE_CHANGE_KEY}} leave the reference query slow after ANALYZE? What did {{LOCK_KEY}} prove about the blocker, why is the missing composite index the next action, and what should a future migration do differently?"
}
